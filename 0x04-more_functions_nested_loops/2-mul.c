#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies to numbers
 * @a: first number
 * @b: second number
 *
 * Return: int product
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
