#include "main.h"
#include <stdio.h>

/**
 * add - adds two numbers
 * @o: first number to add
 * @j: second number to add
 *
 * Return: int value
 */

int add(int o, int j)
{
	int value;

	value = 0;

	value = o + j;

	return (value);
}

